<!-- indexer::stop -->
{% if not isAjaxRequest %}

    {% do addJavascriptResource('assets/contao-component-vue-js/vue/dist/vue.min.js|static') %}
    {% do addJavascriptResource('https://cdnjs.cloudflare.com/ajax/libs/lodash.js/4.17.15/lodash.min.js') %}
    {% do addJavascriptResource('bundles/markocupicsaceventtool/js/eventstory_list_vue.min.js|static') %}

    <div class="{{ class }} block" {{ cssID }}{% if style is defined %} style="{{ style }}"{% endif %}>

        <!-- headline start -->{% if headline is defined %}<{{ hl }}>{{ headline }}</{{ hl }}>{% endif %}<!-- headline end -->

        <div id="eventStoryList{{ id }}">

            <div class=" list-container" v-html="listContent"></div>

            <!-- start modal -->
            <div class="modal" class="event-story-reader-modal" tabindex="-1" role="dialog" aria-labelledby="eventStoryReaderModalTitle{{ id }}" aria-hidden="true">
                <div class="modal-dialog modal-xl modal-fullscreen-xl-down" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="eventStoryReaderModalTitle{{ id }}">Tourenberichte</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Schliessen"></button>
                        </div>

                        <div class="modal-body">
                            <div class="d-flex justify-content-end">
                                <button type="button" v-if="hasPrevItem()" v-on:click="goToPrevItem()" class="btn btn-primary ms-0"><i class="fas fa-arrow-left"></i> Vorheriger Bericht</button>
                                <button type="button" v-if="hasNextItem()" v-on:click="goToNextItem()" class="btn btn-primary ms-2">Nächster Bericht <i class="fas fa-arrow-right"></i></button>
                            </div>

                            <div v-html="readerContent">
                                <div v-html="readerContent"></div>
                            </div>
                        </div>

                        <div class="modal-footer">
                            <button type="button" class="btn btn-primary" data-bs-dismiss="modal">Schliessen</button>
                        </div>
                    </div>
                </div>
            </div>
            <!-- end modal -->
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            // Instantiate ItemWatcher class
            new ItemWatcher('#eventStoryList{{ id }}', {
                'params': {
                    'listModuleId': '{{ id }}',
                    'readerModuleId': '336',
                    'perPage': '{{ perPage }}',
                    'itemIds': [{{ arrAllIds|join(',') }}],
                    'language': '{{ language }}',
                }
            });
        }, false);
    </script>

{% endif %}

{% if isAjaxRequest %}
    <div class="gallery-grid equal-height block">
        {% for story in stories %}
            <div itemscope itemtype="http://schema.org/Article">
                <div class="card event-item">

                    <div class="p-2 d-flex align-items-center">
                        {{ '{{image::' ~ getAvatar.invoke(story.authorId, 'FE') ~ '?width=32&height=32&mode=crop&class=rounded-circle}}' }}
                        <p class="card-text fw-bold small m-0 ms-2">von {{ story.authorName }}</p>
                    </div>

                    {% if story.singleSRC %}
                        <div class="image-zoom-in">
                            <a href="{{ story.href }}" class="item-reader-link">
                                {{ '{{image::' ~ story.singleSRC.uuid ~ '?width=450&height=300&mode=crop&class=card-img-top}}' }}
                            </a>
                        </div>
                    {% endif %}

                    <div class="card-body">
                        <p class="card-date m-0">
                            <time datetime="{{ story.dateAdded|date('Y-m-d\TH:i:sP') }}">
                                <small>{{ story.dateAdded|date('d.m.Y') }}</small>
                            </time>
                        </p>

                        <a href="{{ story.href }}" title="weiterlesen" class="text-decoration-none item-reader-link">
                            <h6 class="card-title mt-0">{{ story.title }}</h6>
                        </a>

                        <p class="card-news-teaser">{{ story.text|raw[0:80] }}</p>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>

    {{ pagination|raw }}
{% endif %}
<!-- indexer::continue -->




