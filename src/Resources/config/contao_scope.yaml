services:

    # Get the contao scope (frontend or backend)
    Markocupic\SacEventToolBundle\ContaoScope\ContaoScope:
        public: true
        arguments:
            - '@contao.framework'
            - '@request_stack'
            - '@contao.routing.scope_matcher'

