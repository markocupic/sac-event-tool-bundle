{%- if renderEmailSubject|default -%}Details zu {{ event.title|default('')|raw }}{%- endif -%}
{% if renderEmailText|default %}
Liebe Teilnehmende oder Angemeldete

Hier kommt der Inhalt deiner E-Mail-Nachricht...

Beste Grüsse

{{ user.firstname }} {{ user.lastname }}

{{ user.mobile|default('') }}
{{ user.email|default('') }}


--------------------------
{{ event.title|raw }}
{{ event_url }}
{% endif %}
